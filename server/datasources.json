{
  "db": {
    "name": "db",
    "connector": "memory"
  },
  "mysqlDs": {
    "host": "localhost",
    "port": 3306,
    "url": "",
    "database": "${DXD_DATABASE}",
    "password": "${DXD_PASSWORD}",
    "name": "mysqlDs",
    "user": "${DXD_USERNAME}",
    "connector": "mysql"
  }
}
