import api from '@/api'
// import {} from './types'

const actions = {
  /* [] ({ commit, dispatch }, id) {
   *   return new Promise((resolve,reject)=>{resolve()})
   * }
   */
}

export default actions
